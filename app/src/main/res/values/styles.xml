<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDodgerBlue</item>
        <item name="colorPrimaryDark">@color/colorDodgerBlue</item>
        <item name="colorAccent">@color/colorDodgerBlue</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolTipRepeat">
        <item name="android:text">@string/This_will_override_your_repeat_reminder</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">16sp</item>
        <item name="android:lineSpacingExtra">5dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="backgroundColor">@color/colorYellow</item>
        <item name="cornerRadius">10dp</item>
    </style>

    <style name="ToolTipOneTime">
        <item name="android:text">@string/This_will_override_your_repeat_reminder</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:lineSpacingExtra">5dp</item>
        <item name="backgroundColor">@color/colorYellow</item>
        <item name="cornerRadius">10dp</item>
    </style>

</resources>
